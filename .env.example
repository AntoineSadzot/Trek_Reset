# Required for high-fidelity hiking routes (OpenRouteService).
# Without this key, the app falls back to straight-line legs.
VITE_ORS_API_KEY=

# For GitHub Pages project sites: /<repo-name>/
# For local/dev or custom domain root: /
VITE_BASE_PATH=/
